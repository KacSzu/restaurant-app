import Button from "../../ui/Button";

function Logout() {
  return (
    <div className="pt-12 text-center">
      <Button variation="logout">Logout</Button>
    </div>
  );
}

export default Logout;
